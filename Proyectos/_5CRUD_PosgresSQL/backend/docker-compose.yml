
# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    image: postgres
    restart: always
    container_name: postgres1

    environment:
      POSTGRES_USER: caleb
      POSTGRES_PASSWORD: 12345
      POSRES_DB: Test

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080

